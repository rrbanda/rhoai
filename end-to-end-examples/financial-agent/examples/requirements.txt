# Data generation (Steps 01-02)
sdg-hub[dev]
pandas>=2.0
pyarrow>=14.0
numpy>=1.26
python-dotenv>=1.0
nest-asyncio>=1.6

# Training — local (Step 03)
training-hub[lora]

# Training — KFP pipeline (Step 03b)
kfp==2.15.2
kfp-kubernetes==2.15.2

# Deployment (Step 04)
kubernetes>=28.0
requests>=2.31

# Evaluation (Step 05)
datasets>=2.18
tabulate>=0.9

# Guardrails (Step 06)
pyyaml>=6.0

# MCP server interaction
fastmcp>=2.0
httpx>=0.28

# Deep Agent harness (Step 07)
deepagents>=0.4.11
langchain>=1.3.10
langchain-openai>=0.3
langgraph-cli[inmem]>=0.4.18
